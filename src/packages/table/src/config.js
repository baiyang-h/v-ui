// 配置文件
export default {
  // 展开行 el-table-column 是否固定, 固定位置
  expandFixed: true,  // true / 'left' / 'right'
  // 展开行 el-table-column 的宽度
  expandWidth:60,
  // 复选框
  selectionFixed: true,
  selectionWidth: 50,
  // 序号
  indexFixed: true,
  indexWidth: 50,
  indexLabel: '#'
}